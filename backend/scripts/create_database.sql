-- Create the HealthEase database (MySQL)
-- Run this in MySQL (Workbench / phpMyAdmin / mysql CLI) before running Prisma.

CREATE DATABASE IF NOT EXISTS healthease_db
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;

