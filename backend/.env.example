PORT=4000
JWT_SECRET=change_me_in_production

# MySQL connection string used by Prisma.
# Create a real `backend/.env` with your actual credentials.
# Example (local MySQL):
# DATABASE_URL="mysql://root:yourpassword@localhost:3306/healthease_db"
